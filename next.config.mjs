/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "api.deezer.com",
      "daisyui.com",
      "i.ibb.co",
      "e-cdn-images.dzcdn.net",
    ],
  },
};

export default nextConfig;
